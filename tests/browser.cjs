const {chromium}=require('C:/Users/Joshua_Patrick/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,channel:"msedge"});const page=await browser.newPage({viewport:{width:1440,height:1100}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(process.env.APP_URL||'http://127.0.0.1:5173/');await page.getByRole('button',{name:'New session',exact:true}).click();await page.getByLabel('Session name').fill('Browser verification');await page.getByRole('button',{name:'Start session',exact:true}).click();await page.getByRole('heading',{name:'Which door will it be?'}).waitFor();await page.getByRole('button',{name:'Door 1',exact:true}).click();await page.getByRole('heading',{name:'Stay, or make the switch?'}).waitFor();await page.screenshot({path:'outputs/desktop.png',fullPage:true});await page.getByRole('button',{name:/Switch to door/}).click();await page.getByRole('button',{name:'Next round'}).waitFor();await page.reload();await page.getByRole('button',{name:'Next round'}).waitFor();await page.getByRole('button',{name:'Next round'}).click();await page.getByRole('button',{name:'Door 2',exact:true}).click();await page.getByRole('button',{name:'Stay with door 2'}).click();await page.getByRole('button',{name:'Next round'}).waitFor();await page.getByRole('button',{name:'End game',exact:true}).click();await page.getByRole('button',{name:'End game & save'}).click();await page.getByRole('heading',{name:'Session complete.'}).waitFor();await page.getByRole('tab',{name:'Overall results'}).click();await page.getByRole('button',{name:'Browser verification',exact:true}).waitFor();const dl=page.waitForEvent('download');await page.getByRole('button',{name:'Download JSON'}).click();const file=await dl;assert.match(file.suggestedFilename(),/monty-hall-sessions/);await page.setViewportSize({width:390,height:844});await page.screenshot({path:'outputs/mobile-results.png',fullPage:true});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>window.innerWidth),false);assert.deepEqual(errors,[]);console.log('PASS: start, choose, switch, reload recovery, stay, end, shared archive, JSON download, mobile width, and no browser errors.');await browser.close()})().catch(e=>{console.error(e);process.exit(1)});


